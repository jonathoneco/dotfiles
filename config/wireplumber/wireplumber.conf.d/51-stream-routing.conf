wireplumber.settings = {
  # Keep application streams on the current default output instead of restoring
  # old per-app sink targets such as stale Pro Audio endpoints.
  node.stream.restore-target = false
}
