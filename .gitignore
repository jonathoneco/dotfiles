*.zcompdump*
*.cache/
__pycache__/
*.DS_Store
*.zsh_history
*.zcompdump
*.stylua.toml
*lazy-lock.json
config/tmux/plugins/
config/lazygit/state.yml
config/nvim/spell
.beans/

# Secrets directory - never commit actual secret files
secrets/*

# Runtime/residue
nvim.log
.oh-my-zsh/
.worktrees/
config/zsh/.zsh_sessions/
*.pre-sshtty
# Claude Code writes plugin-synced skills here at runtime; machine state, not tracked.
home/.claude/skills/synced/
# Orca writes these Pi integration extensions itself, version-matched to the
# installed Orca build; machine state, not tracked.
home/.pi/agent/extensions/orca-*.ts
