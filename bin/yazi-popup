#!/bin/bash
# Wrapper for yazi in tmux popup that fixes terminal response timeout

# Set proper terminal - use screen-256color which has fewer capabilities
export TERM=screen-256color
export COLORTERM=truecolor

# Ensure tmux passthrough is enabled for this pane
tmux set -p allow-passthrough on 2>/dev/null

# Run yazi
exec yazi "$@"