#!/bin/sh
# sandlot: the sandlot CLI on garden-pop, named bare on every machine. Routes through
# garden-pop-tool, which runs the same dispatcher as Hermes's sandlot launcher, so
# a skill's commands behave the same here as inside Hermes.
exec "$(dirname "$0")/garden-pop-tool" sandlot "$@"
