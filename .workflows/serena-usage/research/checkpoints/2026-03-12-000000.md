# Checkpoint: research — 2026-03-12

## Accomplished This Session
- Identified root cause of empty Serena dashboards: Claude Code never calls Serena tools because no instructions tell it to (unlike Codex which has explicit activation rules in `default.rules`)
- Classified all ~35 Serena tools: 7-9 add genuine value over Claude Code builtins (symbol navigation, rename, onboarding), rest are redundant
- Researched activation mechanisms: `initial_instructions` is the gateway tool; `activate_project` is disabled in `claude-code` context (auto-activated)
- Designed three-strategy activation approach: SessionStart hook context injection + CLAUDE.md instructions + Serena's own system prompt

## Files Modified
- `.workflows/serena-usage/research/dashboard-no-stats.md` — Root cause analysis of empty dashboard stats
- `.workflows/serena-usage/research/serena-tools-value-analysis.md` — Tool-by-tool comparison with Claude Code builtins
- `.workflows/serena-usage/research/activation-instructions.md` — Activation strategies and implementation approaches
- `.workflows/serena-usage/research/index.md` — Updated with all three research topics

## Remaining Work
- None for research phase — all key questions answered

## Open Questions
- Should `project.yml` exclude redundant tools to reduce token overhead?
- Per-project language configuration: dotfiles only lists `lua`, other projects need their languages
- How should subagents interact with Serena? (CLAUDE.md visible, hook context not)
- Token budget impact of calling `initial_instructions` every session

## Resumption Prompt
> Research phase for the serena-usage workflow is complete. The core problem was that Serena dashboards show "No tool usage stats collected yet" because Claude Code has no instructions to use Serena tools — the server runs but sits idle. Three research notes were produced: root cause analysis (dashboard-no-stats.md), tool value classification (serena-tools-value-analysis.md), and activation strategy design (activation-instructions.md). All are in `.workflows/serena-usage/research/`. The next step is the plan phase — read the handoff prompt at `.workflows/serena-usage/research/handoff-prompt.md` and start with `/workflow-plan serena-usage`.
