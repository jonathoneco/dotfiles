#!/bin/sh
# Runs the fast local checks before every commit. install.sh points
# core.hooksPath here; on a clone it did not set up, run
# `git config core.hooksPath .githooks` once.
set -eu
cd "$(git rev-parse --show-toplevel)"
exec ./validate.sh
